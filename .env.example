# Optional: Analytics
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=your-domain.com

# Note: The chat widget uses client-side API keys
# Users enter their own Claude or Perplexity API keys
# These are stored in localStorage, not sent to the server
